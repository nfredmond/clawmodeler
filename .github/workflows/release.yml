name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x64
          - os: macos-14
            target: macos-arm64
          - os: windows-latest
            target: windows-x64
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Linux system dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libpango-1.0-0 \
            libpangoft2-1.0-0 \
            patchelf \
            libsoup-3.0-dev

      - name: Install macOS WeasyPrint runtime dependencies
        if: matrix.os == 'macos-14'
        run: |
          brew update
          brew install pango fontconfig harfbuzz glib

      - name: Set up Windows WeasyPrint runtime dependencies
        id: msys2
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          path-type: inherit
          install: mingw-w64-x86_64-pango

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install engine
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10 --activate

      - name: Release workflow audit
        run: pnpm release:workflow:test

      - name: Install desktop dependencies
        run: pnpm install --frozen-lockfile
        working-directory: desktop

      - name: Build Tauri bundles
        run: pnpm tauri:build
        working-directory: desktop
        env:
          MSYS2_LOCATION: ${{ steps.msys2.outputs.msys2-location }}
          MSYS2_ROOT: ${{ steps.msys2.outputs.msys2-location }}

      - name: Smoke test packaged sidecar
        run: pnpm release:sidecar-smoke

      - name: First-user packaged smoke
        run: >
          pnpm release:first-user-smoke -- --binary
          "desktop/src-tauri/binaries/clawmodeler-engine${{ matrix.os == 'windows-latest' && '.exe' || '' }}"

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: clawmodeler-${{ matrix.target }}
          if-no-files-found: error
          path: |
            desktop/src-tauri/target/release/bundle/appimage/*.AppImage
            desktop/src-tauri/target/release/bundle/deb/*.deb
            desktop/src-tauri/target/release/bundle/rpm/*.rpm
            desktop/src-tauri/target/release/bundle/dmg/*.dmg
            desktop/src-tauri/target/release/bundle/msi/*.msi
            desktop/src-tauri/target/release/bundle/nsis/*-setup.exe

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10 --activate

      - name: Release workflow audit
        run: pnpm release:workflow:test

      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts
          merge-multiple: true

      - name: Determine latest release policy
        id: latest-policy
        run: node scripts/release-latest-policy.mjs --current "$GITHUB_REF_NAME" --github-output "$GITHUB_OUTPUT"

      - name: Validate release assets
        run: pnpm release:assets -- --tag "$GITHUB_REF_NAME" --dir artifacts

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/**/*
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc.') }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          make_latest: ${{ steps.latest-policy.outputs.make_latest }}
